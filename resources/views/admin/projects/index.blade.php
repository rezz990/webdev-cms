@extends('layouts.admin') @section('title','Project') @section('content')<div class="heading"><h1>Project</h1><a class="button" href="{{ route('admin.projects.create') }}">Buat project</a></div><div class="table-wrap"><table><thead><tr><th>Nama</th><th>Status</th><th>Tahun</th><th>Aksi</th></tr></thead><tbody>@foreach($projects as $project)<tr><td>{{ $project->name }}</td><td>{{ $project->status->value }}</td><td>{{ $project->year }}</td><td><a href="{{ route('admin.projects.edit',$project) }}">Edit</a><form method="post" action="{{ route('admin.projects.destroy',$project) }}" onsubmit="return confirm('Hapus project ini?')">@csrf @method('delete')<button>Hapus</button></form></td></tr>@endforeach</tbody></table></div>{{ $projects->links() }}@endsection
