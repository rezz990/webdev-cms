@extends('layouts.admin') @section('title','Tulisan') @section('content')<div class="heading"><h1>Tulisan</h1><a class="button" href="{{ route('admin.posts.create') }}">Buat tulisan</a></div><form class="filters"><input name="q" value="{{ request('q') }}" placeholder="Cari judul"><button>Cari</button></form><div class="table-wrap"><table><thead><tr><th>Judul</th><th>Status</th><th>Diperbarui</th><th>Aksi</th></tr></thead><tbody>@foreach($posts as $post)<tr><td>{{ $post->title }}</td><td>{{ $post->status->value }}</td><td>{{ $post->updated_at->diffForHumans() }}</td><td><a href="{{ route('admin.posts.edit',$post) }}">Edit</a><form method="post" action="{{ route('admin.posts.destroy',$post) }}" onsubmit="return confirm('Hapus tulisan ini?')">@csrf @method('delete')<button>Hapus</button></form></td></tr>@endforeach</tbody></table></div>{{ $posts->links() }}@endsection
