@extends('layouts.admin') @section('title','Dashboard') @section('content')<div class="heading"><h1>Ringkasan</h1><div class="actions"><a class="button" href="{{ route('admin.posts.create') }}">Tulisan baru</a><a class="button secondary" href="{{ route('admin.projects.create') }}">Project baru</a></div></div><div class="stats"><strong>{{ $projectCount }}<small>Project</small></strong><strong>{{ $publishedCount }}<small>Tulisan terbit</small></strong><strong>{{ $draftCount }}<small>Draft</small></strong><strong>{{ $unreadCount }}<small>Pesan belum dibaca</small></strong></div>@if($scheduled)<div class="notice">Terjadwal berikutnya: {{ $scheduled->title }} — {{ $scheduled->published_at->translatedFormat('d M Y H:i') }}</div>@endif@endsection
