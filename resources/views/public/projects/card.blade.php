<article class="card">@if($project->cover_image)<img src="{{ asset('storage/'.$project->cover_image) }}" alt="Cover {{ $project->name }}" width="640" height="400" loading="lazy">@endif<div class="card-body"><div class="meta"><span>{{ $project->year }}</span><span>{{ $project->project_status }}</span>@if($project->is_featured)<span>Unggulan</span>@endif</div><h3><a href="{{ route('projects.show',$project) }}">{{ $project->name }}</a></h3><p>{{ $project->summary }}</p><div class="chips small">@foreach($project->technologies->take(4) as $technology)<span>{{ $technology->name }}</span>@endforeach</div></div></article>
