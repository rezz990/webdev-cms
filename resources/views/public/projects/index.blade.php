@extends('layouts.public') @section('title','Project — Webdev Reza') @section('content')<section class="page-head"><div class="container"><span class="eyebrow">Portfolio</span><h1>Project yang saya bangun</h1><p>Eksperimen, produk, dan studi kasus dari masalah yang benar-benar saya temui.</p></div></section><section class="section container"><form class="filters" method="get"><label for="category">Kategori</label><select id="category" name="category"><option value="">Semua</option>@foreach($categories as $category)<option value="{{ $category->slug }}" @selected(request('category')===$category->slug)>{{ $category->name }}</option>@endforeach</select><button class="button">Terapkan</button></form><div class="grid">@forelse($projects as $project) @include('public.projects.card') @empty<p class="empty">Belum ada project sesuai filter.</p>@endforelse</div>{{ $projects->links() }}</section>@endsection
